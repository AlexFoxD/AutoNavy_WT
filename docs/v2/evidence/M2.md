# Task 2 report — M2 capture ownership and geometry

## Status and scope

M2 implementation and explicit self-review complete. Worktree: `C:/Develop/game/WT/AutoNavy_WT-v2`; branch: `feature/v2-modernization`; source base: `19ed1b6ea689dc09e0cb9e64e5af6dcefcd374ae`. Scoped local implementation commit recorded below after creation. Coordinator-owned shared records and environment evidence are not staged.

The actual `Application.run` path now calls `autonavy.capture.factory.create_capture(settings)`. Replay remains finite and synchronous; default DXcam constructs an inert supervisor that opens the native camera only in its spawned child during explicit start. OBS raises an actionable unavailable error until M7, with no fallback. The M1 CLI characterization now tests DXcam selection using an injected finite capture factory, never a real camera.

`toolkit/scn.py` retains only pure legacy matching helpers. Its DXcam/Win32 imports, full-desktop fallback, global camera `D`, recording thread and independent reader are removed. The old inactive `firesystem.py` and `toolkit/deg_cal.py` still reference `scn.D`; M3/M5 must migrate them to explicit packets before activating those wrappers. Supported startup paths already use the new Application and cannot enter those legacy owners.

## Exact contracts for subsequent tasks

- `create_capture(settings: Settings) -> Capture`: explicit replay/DXcam selection. Factory construction acquires nothing. M7 can select `ProcessCapture(settings, source_factory=OBSSource)` once its real child-local adapter exists.
- `Capture.start() -> None`, `read(timeout: float | None = None) -> FramePacket | None`, `stop() -> None`, `close() -> None`. `None` means finite EOF or cancellation; `CaptureTimeout(CaptureError(RuntimeError))` means the caller's wait budget expired without a new publication. A healthy static/no-new-frame stream remains started. Native startup/read errors are `CaptureError`. Application continues after no-frame timeout and checks stop each time; true errors return exit 3.
- `ProcessCapture(settings, *, source_factory=None)`. Default factory is `DXcamSource`. `read(timeout=0)` polls; omitted timeout uses `capture.frame_timeout_s`. One runtime reader drains transport; all other consumers retain packet objects or read `capture.latest`. M5 should pass the control tick budget rather than the default one-second wait when action deadlines are active.
- `ProcessCapture.request_stop() -> None` signals cancellation, closes/wakes the latest slot and signals the child without joining. `stop()` adds bounded process reclamation and permits restart. `close()` is terminal/idempotent and reports child cleanup failures after reclaiming it. `started`, `closed`, `process`, `source_generation`, `publication_id`, `latest` are observable. Each restart creates a fresh cancellation event/slot/shared allocation and increments generation; publication_id increases within that generation.
- `LatestFrameSlot.publish(packet)`, `read()`, `wait(*, after: tuple[generation, publication_id] | None = None, timeout=None)`, `close()`. One retained packet; strictly increasing identities; close wakes waiters and rejects later publications. No per-consumer image copy.
- `CaptureSample(image, geometry, received_at_ns)` is the child-local native result. `DXcamSource(settings, *, camera_factory=None, geometry_adapter=None)` implements native `start/read/close`; it is intentionally used only by the supervisor child. `read()` returns `CaptureSample | None`. Fake factories/adapters appear only in tests.
- `FramePacket` preserves all M1 positional fields and adds optional trailing `geometry: GeometrySnapshot | None = None`. If supplied, snapshot identity and frame dimensions must match the packet. Existing synthetic replay manifests retain their geometry_id-only contract and cannot enable input. Live packets always carry the immutable full snapshot.
- `GeometrySnapshot(client_rect, frame_size, content_rect, dpi=96, profile_id='legacy-1280x720', ui_scale=1.0, window_handle=0, profile_size=(1280,720))` normalizes coordinate sequences to immutable tuples. `geometry_id` hashes the complete snapshot. `recognition_supported` only accepts the preserved legacy profile, actual/reference/frame 1280x720, full content, UI scale 1, DPI 96. Mathematical scaling alone never establishes a recognized profile.
- Geometry methods: `frame_to_desktop(point)`, `desktop_to_frame(point)`, `roi_to_desktop(point, roi)`, `frame_box_to_desktop(box)`, `output_region(output_rect)`, `diagnostic()`. Out-of-bounds and nonfinite point inputs reject. Box right/bottom are exclusive boundaries. Diagnostic reports client/frame/content/profile dimensions, effective scale, supported status and proposed center. Application logs it on each new geometry identity from its actual packet.
- `WindowsGeometry(settings: GeometrySettings, *, api=None).snapshot(frame_size)` sets and restores per-thread per-monitor-V2 DPI context, locates the configured window and uses client bounds plus client-to-screen conversion. Missing, hidden, minimized, empty or unmappable windows fail closed. No border guesses or desktop fallback.

## Pinned native API and lifecycle evidence

Inspected installed DXcam 0.0.5 text without importing it: `.venv/Lib/site-packages/dxcam/__init__.py` exposes `create(device_idx=0, output_idx=None, region=None, output_color='RGB', max_buffer_len=64)`; `dxcam.py` exposes `grab(region=None)`, `start(region=None,target_fps=60,video_mode=False,delay=0)`, `get_latest_frame()`, `stop()`, `release()`.

`get_latest_frame` waits on a private event indefinitely. `_on_output_change` retries COM construction indefinitely without cancellation. DXcam module import enumerates adapters. Therefore only the child imports DXcam, creates its camera, invokes `grab(region=...)`, and calls `release()` in finally. The adapter never uses pinned recording `start`, `get_latest_frame`, private stop events, newer APIs, or abandoned daemon threads. The pin is unchanged.

The selected camera's pinned `_output.desc.DesktopCoordinates` supplies the monitor desktop origin. Client bounds are mapped to that output's relative crop, checked before capture, and checked again afterward with a new client snapshot. Moves during the grab discard the result. Unsupported/unestablished output mapping fails closed; no assumption relates a DXcam output index to Win32 monitor enumeration.

Native `grab` runs at managed configured cadence (default 60 FPS, configurable 120); this is a polling target, not measured throughput. Default no-frame returns publish nothing. Explicit artificial-repeat mode caches pixels only under unchanged geometry and carries the original successful receive timestamp unchanged. A genuinely acquired static image gets its real new receive time. A move away and back invalidates the cache, and restart discards it. No source render timestamp or distinct-presentation guarantee is invented.

Transport uses one fixed shared pixel array and an 8192-byte metadata buffer, protected by timed locking. Bounded independent error/cleanup buffers and aligned control words avoid reliance on a mutex a terminated child may own (supported Windows x64). Parent FramePacket makes the ownership copy while locked; metadata and pixels belong to that same publication. There is no full-frame FIFO, multiprocessing Queue feeder, or unbounded buffering. An old in-flight read keeps its original shared lock/generation/cancel event, so close/restart cannot redirect it to new geometry or publication state.

Stop first wakes parent waits and signals the child. The configured stop budget is split between normal join, terminate/join and kill/join; unreclaimed processes raise an explicit error. Time budgets bound waits/escalation, not OS scheduling or memory-copy execution time. Cleanup failures remain distinct from already-reported startup/read errors. Close drops the shared allocation after reclamation; retained FramePackets remain independently valid.

M5 boundary: `Application.stop` calls the non-waiting request_stop when available; `Application.close` is the capture wait boundary. Physical input is absent in M2. M5 must cancel/release input before this boundary, independently of capture errors. Do not put input ownership in the capture process.

## RED / GREEN and self-review evidence

1. Initial unit RED: `.venv/Scripts/python.exe -m pytest tests/unit/test_capture.py tests/unit/test_geometry.py -q --tb=short` — **8 failed**, expected missing ownership/geometry implementation.
2. Initial process RED: `.venv/Scripts/python.exe -m pytest tests/integration/test_capture_runtime.py -q --tb=short` — **7 failed**, expected missing supervised capture.
3. Repeat sample contract RED — **2 failed** because the old tuple result had no geometry/timestamp object. Added CaptureSample and preserved successful time through artificial repeats.
4. First implementation — **13 passed, 2 failed**: close repeated the original startup/read error. Separated runtime and cleanup errors; expanded focused foundations/replay suite — **104 passed**.
5. Legacy import RED — **1 failed** because importing scn reached blocked DXcam import. Removed the independent owner; only pure helpers remain. No native import was allowed through the guard.
6. Profile/reference RED — **1 failed, 21 passed**: configured nonlegacy reference dimensions were incorrectly recognized from pixel shape alone. Added immutable profile_size to snapshot and recognition predicate.
7. Initial full regression — **152 passed, 1 deselected**. Self-review calibration/common-read RED — **2 failed** for missing Application calibration log and replay read timeout parameter. Wired both; focused capture/geometry/runtime/replay GREEN — **68 passed**.
8. Systematic lifecycle race RED — **2 failed** with AttributeError on close and wrong-semaphore release on restart, reproducibly pausing at packet ownership copy. Bound each read to its original transport/cancel/slot/generation and guarded final publication. Supervised-runtime GREEN — **14 passed**.
9. Repeat-cache move-away/return RED — **1 failed**: a no-frame return at old bounds could revive old cached pixels. Geometry change now clears cache immediately before acquisition, not merely suppresses the current return.
10. Final fresh regression: `.venv/Scripts/python.exe -m pytest tests -q -k 'not test_batch_launcher_does_not_permanently_change_execution_policy' --tb=short` — **156 passed, 1 deselected in 18.14 seconds**. This includes 26 M2 tests, all prior safe tests and guarded supported-entrypoint replay/config/help tests.
11. `.venv/Scripts/python.exe -m compileall -q autonavy toolkit/scn.py` — exit 0. Scoped `git diff --check` — exit 0, only ordinary LF/CRLF normalization notices. An earlier extra blank line in scn was removed and check rerun.

Self-review covered sole native ownership, actual factory callers, immutable packet geometry, profile recognition, output mapping, source-time semantics, static no-frame behavior, stop-before-wait, restart identity, abandoned writer lock, startup/read hangs, cleanup error reporting and shared memory reclamation. Coordinator supplies the independent review after this handoff.

## Remaining acceptance limits

- No game, actual camera, OBS, input, global hooks, network requests, installs or system settings were exercised. Real DXGI/window behavior, achieved FPS and recognition calibration remain manual acceptance; deterministic fakes do not establish these.
- Frame silence is not treated as disconnection. A hung native read and a healthy static source both leave the caller with bounded CaptureTimeout; explicit stop reclaims the hung process. True process/native exceptions fail immediately. No pixel-content hashing or fabricated health claims.
- Automatic retries are not introduced: native errors fail closed and explicit stop/start creates a new generation. The existing read_failure_limit setting is reserved for later adapter policy rather than applied to healthy DXcam no-presentation returns.
- M7 must implement OBS through the same actual factory and child-local source boundary. Pure letterbox transforms already exist; OBS source timestamps must be treated honestly.
- M8 must verify frozen multiprocessing spawn/freeze_support and bundle inclusion in the actual packaged entrypoints. M2 verifies plain CPython 3.11 Windows spawn only.
- The known side-effecting launcher policy regression test remained excluded throughout; coordinator owns its M8 correction. No push, merge, branch cleanup or shared-record staging occurred.

## Local commit

Implementation commit: `e7119e08d2080e8dfe288bab42996d1fc0b0bf65` — Add supervised capture ownership and packet-bound client geometry. Sixteen scoped files; no shared coordinator records staged. After commit, only coordinator-owned DECISIONS/PLAN/STATUS/VERIFICATION/M8-investigation and environment-after-M1 evidence remained modified/untracked. Coordinator will retain this ignored task report as evidence.

## Independent review corrections — M2 round 1

The independent review rejected the initial commit for two confirmed lifecycle races. The original in-flight-copy test paused too late to establish a coherent initial read snapshot. No live devices were involved in reproduction or correction.

**P1: read combined attributes from different generations.** A deterministic test pauses the reader immediately after its first lifecycle attribute is loaded (`_shared` in the rejected implementation, `_session` after correction), completes stop/start, and resumes. RED returned old transport pixels with source_generation=2 instead of cancellation. The corrected implementation publishes a single frozen `_Session` reference that owns the process, shared transport, cancellation event, latest slot, settings, generation, and per-session progress. A read captures that reference once; every precondition and all packet metadata derive from it. Final publication checks both its cancellation and that the active session is still that exact object. Restart never rewrites old session references. A subsequent readiness-interleaving test exposed cancellation being misclassified as "not started"; generation readiness is now historical/one-way, while cancellation remains a separate one-way signal.

**P2: stop during capture construction or before start was erased.** Application now installs the constructed capture under a short lock and checks the persistent application stop/closed state before invoking start. Resource creation and startup-ready waits stay outside this lock. A stop between installation and the start call signals the installed source; ProcessCapture keeps that signal latched instead of replacing it. The same latch covers cancellation during shared allocation before the session is installed. The child checks cancellation before constructing its native source and again before native startup. Cancellation during the startup-ready wait wakes immediately; the caller performs only bounded reclamation. A capture startup error that races an already-requested application stop is classified as normal cancellation; genuine cleanup failures still propagate through final cleanup.

**Concurrent close/start:** a separate test reproduced a new child being launched after stop completed but before close marked the source terminal. Close now holds lifecycle exclusion through reclamation and terminal-state publication, marking a closing state first. Request-stop remains signal-only, and the startup-ready wait never holds the lifecycle lock. Failed reclamation leaves the source closing so a subsequent close can retry but start cannot create an orphan. Late startup failures reclaim only their captured session, never whichever generation happens to be current.

Public capture/read/geometry signatures are unchanged. `request_stop()` before initial start now latches cancellation without creating a process. An explicit completed `stop()` still permits a deliberate restart; terminal close does not reset the latch. Observable process/started/latest/generation/publication values are properties of the active generation. The obsolete private `_shared` compatibility accessor was removed; the abandoned-lock test inspects the current session directly.

Verification:

- New deterministic RED: `.venv/Scripts/python.exe -m pytest tests/integration/test_capture_runtime.py -q -k 'snapshot_is_one or installation or terminal_state' --tb=short` — **4 failed**: mixed generation, two canceled startup errors, and close/restart race. The test cleanup reclaimed every fake child.
- First correction GREEN: full capture-runtime suite — **18 passed in 14.72 seconds**.
- Expanded startup/allocation/readiness checks: **1 failed, 6 passed**; only cancellation between readiness preconditions failed. Removed readiness reset during reclamation; canceled readiness can no longer become a lifecycle error.
- Focused capture/geometry/runtime/replay: `.venv/Scripts/python.exe -m pytest tests/integration/test_capture_runtime.py tests/unit/test_capture.py tests/unit/test_geometry.py tests/integration/test_replay.py -q --tb=short` — **79 passed in 22.86 seconds**.
- Permitted full regression: `.venv/Scripts/python.exe -m pytest tests -q -k 'not test_batch_launcher_does_not_permanently_change_execution_policy' --tb=short` — **165 passed, 1 deselected in 29.72 seconds**. Nine new deterministic review regressions; existing offline tests preserved.
- Compileall and scoped diff check — exit 0. No device, network, input, install, or system-setting action; coordinator docs commit `4a3ab0e` is preserved. Only app.py, capture/process.py, and test_capture_runtime.py belong to this correction.

Correction commit: `468ab7202621ebc74f29c6f50fe298453046a59c` — Make capture generation snapshots and startup cancellation coherent. Final capture-runtime check after removing the unused private accessor: **23 passed in 13.67 seconds**. Staged diff check passed; the index contained exactly the three owned files. Git working tree was clean after commit (scratch report remains ignored). Independent re-review remains the coordinator's next gate.

## Independent review corrections — M2 round 2

The scoped re-review confirmed both original findings were addressed, then identified an application cleanup completion race introduced by round 1. `Application.close` marked `_closed` before its capture cleanup returned, so a concurrent run-finally caller could return success/STOPPED while another caller still reclaimed the process. A cleanup exception reached only the external closer and did not set the application's runtime error.

The correction is limited to `autonavy/app.py` and device-free integration tests. Application now distinguishes closing from completed cleanup. One caller owns resource cleanup; all other close callers await a shared completion event and receive the same recorded error. `_closed` is published only after the cleanup attempt completes, with error state published before the event. A failure is observable by the runtime and subsequent close calls; no caller can silently turn a failed cleanup into success. Resource cleanup remains outside the application lock, and `stop`/request-stop remain non-waiting. A concurrent caller's wait is bounded by `capture.stop_timeout_s + runtime.join_timeout_s`; exceeding that allowance raises an explicit cleanup-budget error. The resource owner still uses the capture backend's bounded cleanup contract.

Three deterministic tests gate a fake resource's cleanup while the real Application run-finally path enters close. Success and failure cases assert that run cannot complete or leave STOPPING before the gate is released. They verify one resource-close call, shared failure propagation into `last_error`/ERROR/exit 3, and successful completion into STOPPED/exit 0. A third case holds cleanup beyond a small configured wait allowance and verifies that concurrent close raises promptly instead of waiting indefinitely or returning success. These fixtures create no native process, device, input, or network resource.

Evidence:

- RED: `.venv/Scripts/python.exe -m pytest tests/integration/test_capture_runtime.py -q -k 'external_cleanup or close_wait_budget' --tb=short` — **3 failed in 0.37 seconds**. Both gated runtime cases returned before cleanup; the concurrent close case did not raise a budget error.
- Focused new GREEN, same command — **3 passed, 23 deselected in 0.42 seconds**.
- Capture/application/replay regression: `.venv/Scripts/python.exe -m pytest tests/integration/test_capture_runtime.py tests/integration/test_replay.py tests/unit/test_capture.py -q --tb=short` — **78 passed in 19.67 seconds**.
- Full permitted regression: `.venv/Scripts/python.exe -m pytest tests -q -k 'not test_batch_launcher_does_not_permanently_change_execution_policy' --tb=short` — **168 passed, 1 deselected in 29.93 seconds**.
- `.venv/Scripts/python.exe -m compileall -q autonavy/app.py` and scoped `git diff --check` — exit 0. Only ordinary LF/CRLF normalization notices.

Round-2 commit: `ceee7bb886f7f4b524f2c8560c4efd64dd6f4720` — Share application cleanup completion and failure across callers. Exactly app.py and test_capture_runtime.py were staged; staged diff check passed. Coordinator-owned REVIEW/STATUS/evidence changes remain untouched and are the only tracked dirty files after commit. Independent re-review remains pending.
