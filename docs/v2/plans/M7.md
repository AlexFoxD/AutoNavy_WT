# M7 OBS capture and geometry Implementation Plan

> **For agentic workers:** Execute inline with superpowers:executing-plans and test-driven-development. Design and local execution are approved; no further approval or subagents.

**Goal:** Deliver CAP-02/03 and GEOM-01 through the actual optional OBS runtime, with fakes for every native boundary.

**Architecture:** Factory selects OBSSource inside existing ProcessCapture child. Selected index/API is opened once; delivered uint8 BGR shape is authoritative. Source diagnostics travel in bounded shared metadata. OBS content maps independently to Windows client coordinates; native profile recognition remains restricted to exact unscaled 1280x720, UI 1, DPI 96.

**Tech Stack:** Python 3.11, pinned OpenCV 4.8, NumPy, existing multiprocessing spawn supervisor and pytest.

**Spec:** docs/v2/SPEC.md sections 1–3, CAP-02/03, FRAME-01, GEOM-01; .superpowers/sdd/PLAN/task-7-brief.md.

## Global Constraints
- Windows x64 and CPython 3.11; preserve native ABI, assets, dependency pins and DXcam default.
- Only C:/Develop/game/WT/AutoNavy_WT-v2 on feature/v2-modernization, base fa67da1. No push/merge/tag/release.
- No hardware/game/camera/input/network/OBS settings changes; fake native boundaries only.
- Preserve input cleanup before capture reclamation, latest-only immutable packets, startup cancellation and generation fencing.

## Task 1: Selected OBS acquisition and transport
Files: autonavy/capture/obs.py (new), factory.py, base.py, process.py, config.py; tests/unit/test_obs.py and existing test_capture.py.
Interface: OBSSource(settings, camera_factory=None, geometry_adapter=None) exposes start/read/close. CaptureSample gains optional diagnostic mapping; ProcessCapture.diagnostic returns latest received diagnostic copy.
- [x] RED tests assert missing adapter explicitly, selection inert until start, exact VideoCapture(index, API), failed open release/reopen, bounded failures, invalid dtype/shape/dimensions, identical acquisitions fresh, explicit repeat stale.
```python
assert importlib.util.find_spec('autonavy.capture.obs'), 'OBS source is missing'
source.start()
sample = source.read()
assert sample.image.shape == (900, 1600, 3)
assert sample.diagnostic['requested']['api'] == 'dshow'
```
- [x] Run `.venv/Scripts/python.exe -m pytest tests/unit/test_obs.py -q --tb=short` and record expected RED.
- [x] Implement lazy cv2.VideoCapture only in start; request width/height/fps/convert-RGB, read reported properties/backend, validate actual delivered array and fail with diagnostic context. Count consecutive read failures; repeat last owned sample only below bound and only under same geometry.
- [x] Add diagnostics to bounded JSON metadata, expose on parent only after successful publication and log on new generation. Preserve source timestamps as unknown.
- [x] Run focused tests, record GREEN.

## Task 2: Calibrated mapping and actual consumers
Files: autonavy/geometry.py, autonavy/input/windows.py, autonavy/behavior.py, autonavy/app.py; tests/unit/test_geometry.py, tests/unit/test_obs.py, tests/integration/test_obs_runtime.py.
Interface: source_geometry(settings, snapshot) applies OBS content_rect/source_id; GeometrySnapshot.content_center supplies aim center. Pure profile_to_frame/frame_to_profile transforms support scaling; recognition_supported requires native content/profile/client dimensions, UI 1/DPI 96.
- [x] RED literal asymmetric canvas mappings, negative desktop origins, scaling round trips and inhibited recognition, current-window guard moved/DPI/source mismatch, integrated aim at content center.
```python
assert geometry.content_center == (740, 410)
assert geometry.frame_to_desktop((740, 410)) == (-860, 460)
```
- [x] Implement mapping and center use; retain translated FrameContext ROI callers and M6 navigation identity invalidation.
- [x] Run focused geometry/vision/input/controller tests and record GREEN.

## Task 3: Spawned OBS lifecycle and runtime evidence
Files: tests/integration/test_obs_runtime.py; test-only fake cameras constructed in real child.
- [x] RED production factory selects OBSSource, actual child read publishes owned pixels and diagnostics; stop/restart increments generation. Fake open/read/release hangs are reclaimed within budgets; app releases held fake input before termination.
- [x] Run focused OBS/capture runtime tests; implement only missing integrations and record GREEN.
- [x] Self-review all profile ROI/ammo/zoom/degree/click consumers, no parent camera construction, no device fallback, no timestamp fabrication, no cancellation changes.
- [x] Full permitted suite once: `.venv/Scripts/python.exe -m pytest tests -q -k 'not test_batch_launcher_does_not_permanently_change_execution_policy' --tb=short`.
- [x] Scoped stage/commit owned files only; write .superpowers/sdd/PLAN/task-7-report.md with exact results and SHA.

## M8 setup/diagnostic handoff
Configure capture.backend='obs', device_index=<explicit virtual-camera index>, obs_api='dshow' or 'msmf' (auto delegates API choice for that same index), pixel_format='BGR', width/height equal OBS canvas; geometry.obs_content_rect=[left,top,right,bottom] must describe the selected OBS output's game content. No discovery or fallback camera is opened. Program/Preview/Scene/Source selection is manual and calibration must match it. The first delivered frame logs requested/reported/delivered properties and effective geometry. Requested FPS is not achieved FPS; unknown source timestamp cannot measure game-to-delivery latency. Hardware/setup validation remains NOT RUN.
Primary API docs supplied and checked by coordinator: https://docs.opencv.org/4.8.0/d8/dfe/classcv_1_1VideoCapture.html (set success is not negotiation proof; getBackendName); https://obsproject.com/kb/virtual-camera-guide (output selection). No latency advantage claimed; DXcam stays default.

The reported backend name identifies OpenCV's capture API, not a friendly device name. Device identity is deliberately reported unverified: verify the configured virtual-camera index manually; this application never opens candidate cameras to discover it. Changing Program/Preview/Scene/Source without updating calibration cannot be detected from image metadata. Verify actual content in preview before manually enabling input. Repeated mode is now accepted for OBS as well as DXcam; OBS returns the last owned sample on unsuccessful read only below read_failure_limit and keeps its original receive timestamp. Native read exceptions and malformed frames terminate the generation immediately. No source-render freshness or latency is inferred from identical pixels.

Self-review: all VisionPipeline template searches (including aim, lock, collision and full-profile UI), ammo baseline, and heading use FrameContext.profile_roi. MatchObservation desktop centers feed UI clicks. Zoom uses only owned Shift/right-button actions and has no pixel ROI. BattlePolicy._fight now subtracts content_center. Navigation context still includes source_generation and geometry_id. Shared capture state/lifecycle synchronization is unchanged except bounded diagnostic metadata publication. OBS tests also cover selected API numeric values, reported-mode disagreement, device context in native exceptions, partial-start cleanup failure, and actual input controller rejection for move/DPI/source/content/scaling mutations.

Execution results (Windows x64, CPython 3.11.9, OpenCV 4.8.0.74, NumPy 1.26.0, pytest 8.3.5): initial OBS RED 13 failed / GREEN 13 passed; geometry/transport/aim RED 4 failed, 5 passed / focused GREEN 34 passed. Expanded capture/input/vision/navigation regression 129 passed (44.05s). Native error-context RED 3 failed, 16 passed / focused GREEN 78 passed (9.69s). Invalid-channel diagnostic RED 1 failed, 20 passed / GREEN 21 passed (0.29s). Full permitted suite: 411 passed, 1 known launcher deselected in 61.51s. Hardware capture/input, friendly-name/device identity validation and latency benchmarks NOT RUN.

Final focused factory-selection strengthening: 21 OBS unit tests passed in 0.36s after the full suite; no production changes after full regression. `.venv/Scripts/python.exe -m autonavy --capture obs --check-config --dry-run` exited 0 without camera/network/input.
