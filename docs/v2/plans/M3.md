# M3: Packet-bound vision implementation plan

Approved scope: task 3 in PLAN.md and `.superpowers/sdd/PLAN/task-3-brief.md`.
Base: reviewed code ceee7bb; coordinator records at 987dc61. Only the sibling v2 checkout is writable for this task.

1. Extend the existing legacy characterization with original repository start, aim, lock and collision assets embedded in deterministic synthetic frames. No screenshots or live devices. Verify raw color Canny single-channel/expanded score agreement, cropped HSV equality and morphology identity.
2. Write failing tests for packet-local bounded derivatives, validated ROI translation, template ownership/version caching, explicit invalid diagnostics, score validity, frame/desktop coordinates, degree extraction and opt-in debug copies.
3. Implement `FrameContext`, `TemplateRegistry`, immutable match/degree/runtime observations and `VisionPipeline`. Match ordering is background first. Neighborhood operations use the selected ROI boundary as in legacy fire/heading/collision processing; pixelwise HSV crops exactly. Preserve color-input Canny by default, and all asset pixels.
4. Load and validate every selected asset before constructing/starting capture. Application processes each acquired packet through the pipeline and retains its observations. Runtime gating rejects missing or unsupported geometry; the context can translate unscaled content offsets in preparation for M7.
5. Replace legacy scn/deg_cal/img_map/firesystem resource acquisition with pure or explicit packet-in compatibility helpers. Legacy matching preserves return shapes; degree has no implicit capture. Fire compatibility emits observations only. M5 supplies centralized scheduled actuation.
6. Run focused RED/GREEN, review packet and cache boundaries, then the full permitted offline suite once. Commit only owned production/tests/this plan. Coordinator owns shared evidence and independent review.

Threshold reconciliation: start .9; confirm .95 with queue variant .6; join .9; ordinary menu .8; research .7 / follow-up .6; WT logo queue .6; aim/black-lock .3; warning .2 and crashed .3; ammo .9; statistics .7. Values represented by existing Settings where available; historical distinct variants remain explicit detector observations for M5 policy.

Validation limits: synthetic compositions establish math, coordinates, branch behavior and structural cache reuse; they do not establish real-battle accuracy, arbitrary scaling calibration, hardware latency or safe live actuation.

## Execution evidence

- Characterization corrected before implementation: `cir.png` carries a white RGB plane and the aiming shape in alpha; preserve its `IMREAD_UNCHANGED` load. Other legacy img_map assets retain `IMREAD_COLOR`. Single-channel versus expanded real-asset score surfaces agree within 6e-6 (maximum observed difference 4.77e-6); best locations agree exactly.
- Initial meaningful RED: focused vision unit/integration suite, 19 failed and 4 passed in 1.12 seconds. Missing vision API/runtime observations, capture construction before selected-template validation and legacy import side effects failed.
- Initial GREEN: 23 passed in 5.90 seconds. The embedded start image's synthetic-background score is approximately .90545, identical to the preserved legacy operation; an initial test's unsubstantiated >.95 expectation was replaced with threshold .9 plus direct legacy score equivalence.
- Self-review RED: 2 failed, 31 passed in 15.12 seconds. Ammo baseline revival after move-away/return and configured-profile mismatch were reproduced and corrected. Expanded GREEN: 33 passed in 13.93 seconds.
- Final permitted regression: `.venv/Scripts/python.exe -m pytest tests -q -k 'not test_batch_launcher_does_not_permanently_change_execution_policy' --tb=short` -> 201 passed, 1 deselected in 48.28 seconds. Compileall and scoped diff check passed.
- Boundary policy: fire HSV is cropped before conversion, but Canny receives the cropped mask exactly as before. Collision and black-lock masks also use their legacy selected-ROI boundaries. A seeded test confirms equality with the legacy mask-then-crop Canny and demonstrates that cropped full-frame Canny would differ. Full-frame menu matching retains full-frame Canny.
- Actual runtime caller: `Application.run -> VisionPipeline.observe(packet)`; `last_observations.packet` is the delivered packet object. Geometry-free smoke replay succeeds with explicitly unavailable observations. `VisionPipeline.begin_battle(packet)` stores an owned ammo baseline for M5 and permanently invalidates it on observed generation/geometry changes.

M5 handoff: image recognition emits no input. Consume menu/purchase/end matches and aim/lock/ammo/collision/degree observations, using their parent packet identity/receive time and conservative `recognition_supported` flag. Additional queue confirmation and battle-end start thresholds are separate named observations. The complete API and RED/GREEN details are in the task report; coordinator owns independent review and shared evidence records.
