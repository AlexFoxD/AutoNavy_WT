Implement AutoNavy_WT v2 in the actual repository. This is an execution request, not a request to give me another plan or comparison.

Repository: https://github.com/AlexFoxD/AutoNavy_WT
Architectural reference only: https://github.com/Priler/csgobot
Required new branch: feature/v2-modernization

Read the entire supplied AUTONAVY_V2_SPEC.md, or the embedded specification between BEGIN AUTONAVY V2 SPEC and END AUTONAVY V2 SPEC, before editing. The embedded and standalone specifications are equivalent inputs; do not require both. Do not rely on any previous conversation.

I approve the supplied design, scope, compatibility constraints, safety defaults, milestone sequence, acceptance criteria, and local implementation work. I choose end-to-end execution now. I authorize an isolated worktree, scoped local commits using the configured Git identity, test execution, and ordinary project-local development dependency installation within the host's permissions. Keep the finished branch/worktree locally. Do not push, open a PR, merge, tag, publish a release, or modify my source branch. No real game input, live matchmaking, purchases, driver installation, or global system changes during verification.

Use the installed Superpowers workflow and the host's real tools. Discover the available skills instead of inventing commands. Review this supplied approved design, establish Git isolation, write concrete milestone plans, and execute them with test-first development, systematic debugging, review, and evidence-based verification. Prefer subagent-driven development when available; otherwise execute sequentially in this session. Ordinary implementation choices and execution-mode selection are already delegated to you. Do not reopen settled design questions or stop at the plan. Respect any higher-priority or mandatory host approval that cannot be satisfied by this explicit approval; never bypass permissions.

Before any project-file edit, inspect the current checkout, source SHA, dirty/untracked files, existing branches/worktrees and repository instructions. Establish feature/v2-modernization from the current committed HEAD. Prefer a host-native isolated worktree; otherwise use a sibling worktree such as ../AutoNavy_WT-v2. Preserve my initial worktree and uncommitted changes. Resume an existing matching v2 task only when its identity is clear; otherwise use the first free numbered branch suffix and report it. Do not reset, stash, clean, rebase or overwrite unrelated work.

On the v2 branch, save the specification as docs/v2/SPEC.md and maintain PLAN.md, STATUS.md, AUDIT.md, DECISIONS.md, VERIFICATION.md, BENCHMARKS.md, MIGRATION.md and REVIEW.md in docs/v2/. Inspect the actual source and reconcile audit targets rather than assuming old findings still apply. Record exact source/target commits and environment constraints.

Implement all mandatory milestones M0-M9. In particular: keep DXcam as the default and implement a real optional OBS backend; remove redundant image conversions and preprocessing; introduce stable latest-frame ownership, explicit geometry and fresh telemetry snapshots; replace unsafe thread termination and blocking action sequences; centralize keyboard/mouse/vJoy actuation; repair ordered route following and angular/controller semantics; integrate the real launcher; deliver replay tests, benchmarks, configuration, English documentation, CI/build updates and a verified handoff. Preserve the native pathfinder ABI and template assets. Do not replace the project with YOLO or copy the donor wholesale.

All your updates, plans, worker prompts, code comments/docstrings, commit messages, documentation and final report must be in English. Preserve external protocol literals, template pixels, resource names and third-party notices verbatim.

For each task, run the relevant test-first or characterization workflow, integrate the implementation into the actual runtime, review spec compliance and code quality, fix findings, and make a scoped commit. Update STATUS.md at each milestone and before context compaction. After compaction, re-read the records and continue instead of starting over. Reviewers must check real callers and evidence, not just the existence of new classes.

Do not claim success based on mocks alone, fabricate benchmark results, mark unrun tests as passed, or weaken tests to hide regressions. Missing Windows/OBS/vJoy/game hardware is a reason to mark specific hardware checks NOT RUN, not a reason to leave required adapters as stubs. Complete independent work and preserve a precise checkpoint for genuine blockers. A host/context limit does not count as task completion.

Finish with the spec's English handoff: implementation/validation status, branch and absolute worktree path, source/final commits, actual changed components, exact commands and test outcomes, measured performance only, working launch/stop/rollback instructions, and remaining risks or manual checks. Leave the source branch untouched and the v2 branch retained.

Begin by reporting the inspected source branch/commit and the v2 workspace decision, then execute the approved workflow. Continue through implementation and verification; do not end by asking whether to start.
